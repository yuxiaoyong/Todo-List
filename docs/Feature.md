# 已实现功能

> 最后更新：2026-06-15  
> 本文档描述**当前代码中已可用**的能力，不含规划项。

---

## 1. 任务管理

| 功能 | 说明 |
|------|------|
| 创建任务 | 快速输入栏、新建对话框、快速捕获窗口、极简模式命令输入 |
| 编辑任务 | 详情面板 / 独立详情窗口，标题、富文本正文、优先级、开始 / 截止日、负责人 |
| 完成状态 | 列表 / 看板 / 极简列表一键切换 |
| 置顶 | 星标置顶，列表与极简模式分区展示（置顶区不参与拖拽排序） |
| 优先级 | 高 / 中 / 低 |
| 截止日期 | 日期选择（`YYYY-MM-DD`），过期样式标识 |
| 开始日期 | 可选，与周期提醒锚定配合 |
| 软删除 | 删除后进入回收站，支持 5 秒内撤销 |
| 回收站 | 查看已删任务、恢复、永久删除、清空回收站 |
| 自动保存 | 详情编辑 3 秒防抖自动保存 |
| 拖拽排序 | 列表视图（默认排序时）、极简列表（`vue-draggable-plus`，带动画） |
| 看板列归属 | 任务可拖入看板列，支持未分配列 |

**相关文件**：`TaskListPanel.vue`、`DraggableTaskList.vue`、`TaskListColumnCells.vue`、`TaskListRowCells.vue`、`TaskListColumnSettings.vue`、`taskListColumns.ts`、`stores/taskListColumns.ts`、`TaskDetailPanel.vue`、`TaskDetailSubtasks.vue`、`MinimalTaskList.vue`、`todo.ts`（store）、`repositories/mod.rs`

---

## 1.1 子任务

| 功能 | 说明 |
|------|------|
| 增删改 | 详情面板 / 独立详情窗口内管理 |
| 完成勾选 | 单项切换，进度条展示完成比例 |
| 持久化 | `subtasks` 表，随任务软删除级联 |

**相关文件**：`TaskDetailSubtasks.vue`、`subtaskApi`、`repositories/mod.rs`

---

## 1.2 周期提醒

| 功能 | 说明 |
|------|------|
| 公历重复 | 日 / 周 / 月 / 季 / 年，可设间隔 |
| 农历重复 | 年度农历日期，支持闰月与节日预设 |
| 锚定日期 | 基于开始日或截止日计算下次提醒 |
| 提醒时间 | 可配置时刻与提前分钟数 |
| 完成后行为 | 自动推进下一周期或保持当前周期 |
| 通知联动 | 与到期提醒共用系统 / 邮件 / 应用内通道 |

**相关文件**：`TaskDetailReminderPanel.vue`、`recurrence.ts`、`domain/recurrence.rs`、`domain/lunar.rs`、`infra/notifications.rs`

---

## 2. 分类与标签

| 功能 | 说明 |
|------|------|
| 分类 | 创建、删除、颜色、侧边栏筛选，显示未完成 / 总数 |
| 标签 | 创建、删除、多标签 OR 筛选 |
| 看板列 | 创建、重命名、删除、排序、颜色、副标题；默认四象限列 |

> 注：分类 / 标签的后端支持更新与排序，侧边栏 UI 仍以新建 / 删除为主（见 [Plan.md](./Plan.md)）。

---

## 3. 视图与筛选

| 功能 | 说明 |
|------|------|
| 列表视图 | 自定义表格（非 `el-table`）：可排序列、完成勾选、行内编辑、列显隐与顺序配置 |
| 列表固定列 | 左固定（置顶 / 勾选 / 标题）、中间横向滚动、右固定（操作）；表头纵向 sticky |
| 列表可选列 | 优先级、状态、开始 / 截止日、分类、标签、负责人、看板列、周期提醒、创建 / 更新时间 |
| 列表列设置 | 表头右侧按钮：切换列可见性、拖拽调整列顺序；配置持久化 |
| 看板视图 | 按列分桶、列内与跨列拖拽 |
| 甘特图视图 | 与列表、看板并列；日 / 周 / 月粒度、「今天」定位；拖拽调整起止日期 |
| 极简模式 | 独立窗口、边缘吸附、失焦缩边、置顶 / 可排序分区 |
| 时间筛选 | 全部 / 年 / 月 / 周 / 今日（客户端过滤） |
| 组合筛选 | 分类 + 标签 + 完成状态 + 优先级 + 搜索词 |
| 回收站模式 | 侧边栏切换，仅显示已删任务 |

列表实现细节见 [ARCHITECTURE.md § 列表视图实现](./ARCHITECTURE.md#列表视图实现)。

---

## 3.1 AI 助手（可选）

> 默认关闭；需在设置 → AI 配置中启用并填写 API 或本地 Ollama。

| 功能 | 说明 |
|------|------|
| 网关配置 | 云端 OpenAI 兼容 API 或本地 Ollama；连接测试与超时 |
| 一句话建任务 | 快速输入栏解析自然语言，预览确认后创建 |
| 子任务拆解 | 详情子任务 Tab 内 AI 生成步骤列表，勾选后追加 |

**相关文件**：`SettingsAiSection.vue`、`stores/aiGateway.ts`、`gateway/ai.rs`

---

## 4. 搜索

| 功能 | 说明 |
|------|------|
| 全文搜索 | SQLite FTS5 |
| 中文分词 | jieba-rs 索引与查询 |
| 搜索范围 | 标题、正文纯文本、标签文本 |
| 防抖查询 | 前端输入防抖后请求后端 |

> 注：搜索结果**尚未**高亮匹配词（见 [Plan.md](./Plan.md)）。

---

## 5. 富文本与附件

| 功能 | 说明 |
|------|------|
| 编辑器 | TipTap（加粗、斜体、下划线、列表、图片） |
| 图片 | 粘贴、拖拽、插入；可调大小 |
| 链接扩展 | 已加载 Link 扩展（工具栏按钮待补） |
| 消毒 | DOMPurify 处理 HTML |
| 附件类型 | 图片、PDF、Office、纯文本等 |
| 附件操作 | 上传、列表、预览、打开本地文件、删除 |
| 存储 | 文件存 `attachments/{todo_id}/`，库内 `local://` 引用 |

---

## 6. 桌面集成

| 功能 | 说明 |
|------|------|
| 系统托盘 | 图标、未完成数量提示、右键菜单（快速添加 / 显示主窗口 / 退出） |
| 单实例 | 重复启动时聚焦已有实例 |
| 窗口状态记忆 | 主窗口位置与尺寸（`tauri-plugin-window-state`） |
| 快速捕获 | 置顶小窗，全局快捷键唤起 |
| 主 / 极简切换 | 全局快捷键，互斥显示 |
| 极简边缘吸附 | 靠左右边缘自动吸附，失焦缩为细条，点击展开 |
| 任务栏隐藏 | 极简模式下主窗口与极简窗口均不占任务栏 |
| 独立详情窗 | 可从列表 / 极简 / 通知打开单独任务详情 |
| 窗口透明度 | 50%–100%，对所有窗口生效 |

---

## 7. 通知与邮件

| 功能 | 说明 |
|------|------|
| 到期提醒总开关 | 设置 → 通知 |
| 系统通知 | Windows 通知中心 Toast，可点击打开任务 |
| 邮件通知 | SMTP 网关，到期时发送邮件 |
| 提前提醒 | 0–168 小时可选 |
| 重复提醒 | 15 分钟–24 小时间隔 |
| 应用内提醒 | 窗口可见时 Element Plus 通知（随总开关） |
| 邮件网关配置 | 服务器、端口、加密、认证方式（无 / 密码 / 授权码）、发件人、默认收件人、测试邮件 |

**调度**：后端每 60 秒检查一次到期与周期任务。

> 注：系统通知 / 邮件主题等 Rust 层文案仍为中文硬编码；无免打扰时段（见 [Plan.md](./Plan.md)）。

---

## 8. 数据管理

| 功能 | 说明 |
|------|------|
| Zip 备份 | 导出数据库、附件、设置为单个 Zip |
| Zip 恢复 | 从备份包恢复（含确认提示） |
| JSON 导出 | 导出分类、标签、看板列、任务、设置快照 |
| JSON 导入 | 合并导入，同名分类 / 标签复用，同标题 + 分类任务跳过 |
| 打开数据目录 | 设置中一键打开应用数据文件夹 |
| 数据统计 | 展示任务数、附件占用等概要信息 |

> 注：无定时 / 退出自动备份（见 [Plan.md](./Plan.md)）。

---

## 9. 设置与个性化

| 分区 | 内容 |
|------|------|
| 外观 | 浅色 / 深色 / 跟随系统；窗口透明度 |
| 语言 | 简体中文 / English，Element Plus 组件语言同步 |
| 快捷键 | 自定义快速捕获、主 / 极简切换；展示应用内固定快捷键 |
| 通知 | 总开关、系统、邮件、提前提醒、重复频率 |
| 邮件 | SMTP 网关完整配置 |
| 数据 | 备份、恢复、JSON 导入导出、打开数据目录 |
| AI | 可选：云端 / Ollama 网关、连接测试 |

**跨窗口同步**：主题、语言、透明度、通知设置等通过 Tauri 事件广播。

---

## 10. 国际化

| 范围 | 语言 |
|------|------|
| 前端 UI | 简体中文、English（`zh-CN.ts` / `en.ts`） |
| 后端系统文案 | 部分仍为中文（托盘菜单、通知标题、窗口标题等） |

---

## 11. 技术特性

| 特性 | 说明 |
|------|------|
| 架构分层 | Vue → Pinia → API → Commands → Repositories → SQLite |
| 数据库迁移 | 版本化 SQL 迁移（001–011），WAL 模式 |
| 多窗口 | 单前端工程 + Hash 路由 + 多 `WebviewWindow` |
| 事件总线 | `todo-changed`、`theme-changed`、`notification-settings-changed` 等 |
| 错误处理 | Rust `AppError` / `AppResult`；前端关键操作有 `ElMessage` 反馈 |

---

## 12. 明确未实现（勿当作已有能力）

以下在代码或文档中出现过痕迹，但**当前不可用**或**不完整**：

- 日历视图
- 命令面板（`Ctrl+K`）
- 任务模板、自然语言日期解析
- 「今日 / 已过期」智能侧边栏入口（`dueToday` API 存在，UI 未接）
- 搜索关键词高亮
- 分类 / 标签侧边栏重命名与颜色编辑
- 免打扰时段、通知文案多语言（Rust 层）
- 自动备份、DB 损坏恢复引导
- 列表虚拟滚动（列配置与固定列已实现，大数据量性能仍待优化）
- 云同步、多人协作、插件体系
- `quadrant` 四象限字段（已迁移为看板列，旧字段保留）
- `TodoItem.vue`（遗留组件，已被 `DraggableTaskList` 替代，待删除）

后续规划见 [Plan.md](./Plan.md)。
